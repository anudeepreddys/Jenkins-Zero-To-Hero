pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Linux') {
      steps {
        sh 'echo $USER'
      }
    }
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
